# enable test run
CIBUILD_TEST_ENABLED=1

# for demo build also arm64
CIBUILD_BUILD_PLATFORMS=linux/amd64,linux/arm64

# github ci does not inject env vars to actions ci
CIBUILD_BUILD_COSIGN_NEW_BUNDLE_FORMAT=0
#CIBUILD_RELEASE_COSIGN_SIGNING_MODE=keyless
CIBUILD_LOG_LEVEL=3